tryCatch(library(anndata),warning = function(w){print("waringing on library anndata")},error=function(e){print("error on library anndata")} )

tryCatch(library(scater),warning = function(w){print("waringing on library scater")},error=function(e){print("error on library scater")} )

tryCatch(library(Seurat),warning = function(w){print("waringing on library Seurat")},error=function(e){print("error on library Seurat")} )

tryCatch(library(cowplot),warning = function(w){print("waringing on library cowplot")},error=function(e){print("error on library cowplot")} )

tryCatch(library(reticulate),warning = function(w){print("waringing on library reticulate")},error=function(e){print("error on library reticulate")} )

tryCatch(library(loomR),warning = function(w){print("waringing on library loomR")},error=function(e){print("error on library loomR")} )

tryCatch(library(monocle),warning = function(w){print("waringing on library monocle")},error=function(e){print("error on library monocle")} )

tryCatch(library(reticulate),warning = function(w){print("waringing on library reticulate")},error=function(e){print("error on library reticulate")} )


tryCatch(library(cellchat),warning = function(w){print("waringing on library dior")},error=function(e){print("error on library cellchat")} )